create a project folder
mkdir -p vagrant/ubuntu
cd vagrant
ls

initialise vagrant with ubuntu box
vagrant init bento/ubuntu-22.04
VagrantFile is created

configuration of VagrantFile
# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|

  config.vm.box = "bento/ubuntu-22.04"

  config.vm.provider "virtualbox" do |vb|
    vb.name   = "ubuntu-devops"
    vb.memory = "2048"
    vb.cpus   = 2
    vb.gui    = false
  end

end

Vagrant.configure("2") do |config|
  config.vm.box = "bento/ubuntu-22.04"

  config.vm.provider "virtualbox" do |vb|
    vb.memory = "1024"
    vb.cpus = 1
  end
end

Vagrantfile → Manages all VM configurations.
vagrant up → Creates and starts the VM.
vagrant ssh → Connects to the VM.
vagrant halt → Stops the VM.
vagrant destroy → Deletes the VM.

vagrant global-status

whoami

cat /etc/os-release

Linux – An open-source operating system kernel created by Linus Torvalds that forms the base of many operating systems.

Debian – A stable and free Linux distribution based on the Linux kernel and using the APT package manager.

Red Hat – A company that provides enterprise Linux distributions like Red Hat Enterprise Linux for servers and businesses.

EXAMPLES ->
Linux (kernel) examples: Ubuntu, Debian, Fedora, Kali Linux

Debian-based distributions examples: Ubuntu, Linux Mint, Kali Linux

Red Hat-based distributions examples: Red Hat Enterprise Linux, CentOS, Fedora, Rocky Linux

vagrant@vagrant:~$ 
 (user)@(hostname):directoryUSER_NORMAL_SHELL

sudo -i 
will take me to root user

cd sbin
ls (system level things)